layer: application
tasks:
  dev:
    env:
      DEVTOOLS_PORT: '42071'
      SERVER_PORT: '5174'
    deps:
      - target: "pear:dev"
      - target: "funnel:start"
        args: ["5174", "$project"]
    options:
      mergeDeps: replace

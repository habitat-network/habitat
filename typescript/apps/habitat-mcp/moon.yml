layer: application
tasks:
  build:
    command: pnpm build
    inputs:
      - src/**/*
      - tsconfig.json
    outputs:
      - dist/**/*
  dev:
    command: node dist/index.js
    deps:
      - ~:build
  test:
    command: pnpm test
    inputs:
      - src/**/*
      - tsconfig.json

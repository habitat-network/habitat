layer: application
tasks:
  dev:
    env:
      DEVTOOLS_PORT: '42072'
      SERVER_PORT: '5175'
    deps:
      - target: "pear:dev"
      - target: "funnel:start"
        args: ["5175", "$project"]
    options:
      mergeDeps: replace

layer: application
workspace:
  inheritedTasks:
    exclude:
      - test
tasks:
  build:
    args:
      - --outDir
      - $workspaceRoot/internal/webui/dist
    deps:
      - typescript/internal:build
    options:
      mergeDeps: replace

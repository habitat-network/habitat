language: typescript

fileGroups:
  lexicons:
    - /lexicons/**/*.json


workspace:
  inheritedTasks:
    exclude:
    - start
    - dev
    - lint
    - lint-check
    - format
    - format-check
tasks:
  build:
    deps: []
    options:
      mergeDeps: replace
  generate:
    command: pnpx 
    args:
    - "@atproto/lex-cli"
    - gen-api
    - $projectRoot
    - "@files(lexicons)"
    outputs:
    - types/
    options:
      interactive: true
    preset: server

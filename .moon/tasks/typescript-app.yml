tasks:
  dev:
    args:
      - --
      - --domain
      - $project.${TS_DOMAIN}
    env:
      HABITAT_DOMAIN: pear.local.habitat.network
    deps:
      - target: "pear:dev"
      - target: "funnel:start"
        args: ["5173", "$project"]
  regenerate:
    command: moon 
    args:
      - generate
      - web-app
      - --to
      - $projectSource
      - --force
      - --
      - --name
      - $project
    preset: utility
    options:
      runFromWorkspaceRoot: true

inheritedBy:
  toolchain: typescript
  layer: application

tasks:
  dev:
    command: 'noop'
    deps:
      - target: 'privi:run'
        env: 
          HABITAT_PORT: '8000'
          HABITAT_DEBUG: 'true'
      - target: 'funnel:run'
        args: ['8000', 'privi']
      - target: 'funnel:run'
        args: ['5173', 'frontend']
      - target: 'frontend:start'

tasks:
  build:
    deps:
    - root:build
  dev:
    deps:
      - root:caddy
      - pear:dev
    env:
      HOME_PORT: '2600'
      HOME_DOMAIN: 'home.local.habitat.network'
      HOME_DB: 'sqlite://home.db'
      # The org home is hardcoded to manage.
      HOME_ORG: 'did:web:pear.local.habitat.network'
      HOME_SECRET: 'z42tt1ZWxkfKn5ujwLsELfY7191h4q6UCFjeRGf6tKXaMCnX'

tasks:
  build:
    deps:
    - root:build
  dev:
    args:
    - --
    - --domain
    - calendar-server.${TS_DOMAIN}
    env:
      CALENDAR_PORT: '8801'
      DEBUG: 'true'
    deps:
    - target: funnel:start
      args:
      - '8801'
      - calendar-server

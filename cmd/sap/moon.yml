tasks:
  dev:
    deps:
      - root:caddy
      - pear:dev
    env:
      SAP_SECRET: 'z42tt1ZWxkfKn5ujwLsELfY7191h4q6UCFjeRGf6tKXaMCnX'
      SAP_IDENTITY_RESOLVER: 'https://pear.local.habitat.network'
      # Posts outbox events straight to the dev Worker with no secret —
      # nothing else on the machine can reach this loopback URL. Replaces
      # chalk polling/holding a connection open to sap's old /channel
      # WebSocket (see typescript/apps/chalk's SapChannel removal).
      SAP_WEBHOOK_URL: 'http://127.0.0.1:5177/webhook/sap'

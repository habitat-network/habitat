{
  "name": "habitat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "frontend:start": "pnpm --filter frontend start",
    "format": "prettier --write .",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "oxlint": "^1.28.0",
    "prettier": "3.6.2"
  }
}

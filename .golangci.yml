version: "2"
linters:
  default: standard
  enable:
    - bodyclose
    - sloglint
    - gocritic
  settings:
    sloglint:
      static-msg: true
      context: "all"
    gocritic:
      enable-all: true
formatters:
  enable: golines

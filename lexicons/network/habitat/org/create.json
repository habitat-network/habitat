{
    "lexicon": 1,
    "id": "network.habitat.org.create",
    "defs": {
        "main": {
            "type": "procedure",
            "description": "Create a new org with a bootstrap admin member.",
            "input": {
                "encoding": "application/json",
                "schema": {
                    "type": "object",
                    "required": ["domain", "admin_handle", "admin_password"],
                    "properties": {
                        "domain": {
                            "type": "string",
                            "description": "The member subdomain for this org, e.g. acme.example.com."
                        },
                        "admin_handle": {
                            "type": "string",
                            "description": "Internal handle for the bootstrap admin (alphanumeric, 1-50 chars)."
                        },
                        "admin_password": {
                            "type": "string",
                            "description": "Password for the bootstrap admin account."
                        }
                    }
                }
            },
            "output": {
                "encoding": "application/json",
                "schema": {
                    "type": "object",
                    "required": ["org_id", "admin_did", "admin_handle"],
                    "properties": {
                        "org_id": {
                            "type": "string",
                            "description": "The ID of the created org."
                        },
                        "admin_did": {
                            "type": "string",
                            "description": "The DID of the bootstrap admin."
                        },
                        "admin_handle": {
                            "type": "string",
                            "description": "The full handle of the bootstrap admin."
                        }
                    }
                }
            }
        }
    }
}

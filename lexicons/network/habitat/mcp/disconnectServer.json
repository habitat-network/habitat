{
  "lexicon": 1,
  "id": "network.habitat.mcp.disconnectServer",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Remove the caller's stored credential for an org-configured MCP server. Callable by any org member.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "id": {
              "type": "string"
            }
          }
        }
      }
    }
  }
}

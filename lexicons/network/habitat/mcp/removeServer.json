{
  "lexicon": 1,
  "id": "network.habitat.mcp.removeServer",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Remove an org's configured MCP server, along with any stored user credentials for it. Only callable by org admins.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "id": {
              "type": "string"
            }
          }
        }
      }
    }
  }
}

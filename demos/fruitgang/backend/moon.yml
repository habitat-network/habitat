layer: application
tasks:
  dev:
    command: $workspaceRoot/bin/fruitgang-backend
    deps:
      - pear:dev
      - ~:build
    env:
      FG_PORT: "3100"
      FG_DOMAIN: "fruitgang-api.local.habitat.network"
    options:
      envFile:
        - /dev.env
      mergeArgs: replace
    preset: server
  build:
    command: go
    args:
      - build
      - -o
      - $workspaceRoot/bin/fruitgang-backend
      - ./cmd
    inputs:
      - "**/*.go"
      - go.mod
      - go.sum
    outputs:
      - /bin/fruitgang-backend
    options:
      mergeArgs: replace

layer: library
fileGroups:
  internal_src:
    - internal/**/*.go
    - '!internal/**/*_test.go'
  internal_test:
    - internal/**/*.go
tasks:
  build:
    inputs:
      - '@group(mod)'
      - '@group(internal_src)'
  test:
    inputs:
      - '@group(mod)'
      - '@group(internal_test)'
  format:
    inputs:
      - '@group(internal_src)'
  format-check:
    inputs:
      - '@group(internal_src)'
  lint:
    inputs:
      - '@group(internal_test)'
  lint-check:
    inputs:
      - '@group(internal_test)'
  caddy:
    command: caddy
    args:
    - run
    - --config
    - '@in(0)'
    inputs:
    - Caddyfile
    preset: server
